sudo: required

dist: trusty

services:
  - docker

before_script:
  - chmod +x ./Makefile
  - docker pull "brisbanebirdteam/build-env:latest"

script:
  - make deploy

notifications:
  email:
    on_success: change
    on_failure: change
